<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |t| %>
    <li><%= link_to t.title, task_path(t) %>
      <% if t.completed %>
        <span>&#10004;</span>
      <% end %>
    </li>
  <% end %>
</ul>

<%= link_to '＋ Add a new task', tasks_new_path %>
